function slice(arr = [], start = 0, end = 0) {
  return arr.slice(start, end);
}

function includes(arr, item) {
  return arr.indexOf(item) > -1;
}

module.exports = {
  slice: slice,
  includes: includes
};
